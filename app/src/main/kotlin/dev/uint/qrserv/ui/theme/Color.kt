package dev.uint.qrserv.ui.theme

import androidx.compose.ui.graphics.Color

val BrandAccent = Color(0xFF5B5DD5) // secondaryContainer / import FAB
val BrandAccentSoft = Color(0xFFBFB4E5) // secondary
val BrandCanvasLight = Color(0xFFEDE8F3)
val BrandCanvasDark = Color(0xFF252329)
// Cards sit a shade darker than the canvas in both themes, rather than the lighter/"elevated"
// tint Material defaults to -- reads as recessed against the background instead of floating.
val BrandCardLight = Color(0xFFDAD0EC)
val BrandCardDark = Color(0xFF1B1920)
val BrandError = Color(0xFFD32F2F) // shutdown FAB
